Браузер выдает ошибку при попытке подключения к 18.157.179.129
"$wgServer must be set in LocalSettings.php. See https://www.mediawiki.org/wiki/Manual: $wgServer."

Далее указаны действия (в формате: время, команда, описание), которые привели к положительному результату по восстановлению работоспосбности сервера:

9.22 ssh -i .ssh/ich.pem ec2-user@18.157.179.129. Подключаемся через ключ, который лежит в скрытой папки .ssh. Вошли на сервер 

# ПРОБЛЕМА1 - недостаток места на сервере

9.33 sudo du -sh /var/log/*. Смотрим использование диска без деталей в "человеческих" единицах измерения. Нашли 7.5G в /var/log/httpd

9.46 sudo su. Входим как суперадмин, чтобы не дергать sudo перед каждой командой.

9.49 cd /var/log/httpd. Заходим физически в директорию, которая съедает место на сервере.
9.50 ls -lah. Уточнили какой именено файл из папки съедает место: access_log-20260318. 
9.53 echo "" > access_log-20260318. Перезаписываем файл, чтобы очистить информацию внутри него, но при этом его не удалять из системы.
9.54 df -h. Проверили. Удача. Место появилось.
9.55 crontab -l. Смотрим планировщик задач. Нашли процесс-паразит, который каждую минуту записывает лог.
9.57 crontab -e. Ставим значок комментария в фачеле строки #. Закомментировали процесс

# ПРОБЛЕМА1 решена

9.59 exit Выходим из-под суперадмина

# ПРОБЛЕМА2 - ошибки в файле LocalSettings.php 

10.00 find / -type f -name "LocalSettings*" 2>/dev/null. Ищем файл, который не дает открыть страницу сайта сервера.
10.02 cat /var/www/html/mediawiki/LocalSettings.php. Подсмотрели, что файл пуст.
10.04 cat "/home/ec2-user/LocalSettings (5).php". Смотрим в файл-копию. Кавычки из-за пробела в названии файла.
10.05 cp "/home/ec2-user/LocalSettings (5).php" /var/www/html/mediawiki/LocalSettings.php.Копируем инфу из файла-копии в пустой файл запуска.
10.06 Смотрим браузер. Видим редирект на другой IP: 18.153.51.162
10.13 nano /var/www/html/mediawiki/LocalSettings.php. Изменяем значение переменной $wgServer меняем на 18.157.179.129

# ПРОБЛЕМА2 решена
  
